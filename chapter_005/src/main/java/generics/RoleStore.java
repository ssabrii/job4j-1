package generics;

/**
 * RoleStore.
 *
 * @author Max Vanny.
 * @version 5.0
 * @since 0.1
 */
public class RoleStore extends AbstractStore<Role> {
    /**
     * Constructor.
     *
     * @param size size store
     */
    public RoleStore(final int size) {
        super(size);
    }
}
